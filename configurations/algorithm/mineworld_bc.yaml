policy:
  type: vpt_causal
  backbone: resnet62
  pretrained_backbone: true
  backbone_trainable: true
  embed_dim: 768
  ffn_dim: 2048
  n_layers: 6
  n_heads: 8
  dropout: 0.1
  attn_dropout: 0.1
  mem_len: 64
  action_dim: ${dataset.external_cond_dim}
