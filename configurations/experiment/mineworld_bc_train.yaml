defaults:
  - _self_

training:
  batch_size: 32
  epochs: 5
  val_fraction: 0.01
  seed: 0
  checkpoint_interval: 1 
  num_workers: 64
  val_num_workers: 4

optimizer:
  lr: 3e-4
  weight_decay: 1e-4
  betas: [0.9, 0.99]
